@model IEnumerable<SPaPS.Models.ServiceActivity>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Activity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Service)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Activity.ActivityId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Service.ServiceId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ServiceActivityId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ServiceActivityId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ServiceActivityId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
